// Stub – inventory deduction is a future feature
const deductInventoryForOrder = async (items, orderId) => {
  // no-op until inventory tracking is fully wired
};

module.exports = { deductInventoryForOrder };
